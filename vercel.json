{
  "framework": null,
  "installCommand": "node vercel-install.js",
  "buildCommand": "cd artifacts/aw't landing in awdp-site's node_modules. Quickest fix — edit **`vercel.json`** only. Replace the `buildCommand` linedp-site && npm install tailwindcss --legacy-peer-deps && npx vite build",
  "outputDirectory": "artifacts/awdp-site/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
