{
  "name": "awdp",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "dependencies": {
    "nodemailer": "^9.0.1",
    "pg": "^8.13.0"
  },
  "scripts": {
    "dev": "pnpm --filter awdp-site run dev",
    "build": "pnpm --filter awdp-site run build",
    "preview": "pnpm --filter awdp-site run serve",
    "vercel:set-api": "node scripts/vercel-set-api-origin.mjs",
    "test": "node --test tests/ssr.test.mjs"
  },
  "engines": {
    "node": ">=20.x"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "uuid": "11.1.1",
      "esbuild": "0.25.8"
    }
  }
}
